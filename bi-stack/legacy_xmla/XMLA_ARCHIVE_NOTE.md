# XMLA/eMondrian archive

This directory preserves the previous XMLA/eMondrian/Pivot4J-based BI stack.

It is no longer the main execution path.

The main BI stack is migrated to BI direct because XMLA/eMondrian introduced persistent catalog discovery and catalog binding failures while the MCAD evaluation model and the analytical data remained valid.
