services:
  mcad-proxy:
    build:
      context: ./mcad-proxy
      dockerfile: Dockerfile.r3-b2
    command:
      - uvicorn
      - r3_measurement_app:app
      - --host
      - 0.0.0.0
      - --port
      - '9000'
      - --log-level
      - info
      - --access-log
