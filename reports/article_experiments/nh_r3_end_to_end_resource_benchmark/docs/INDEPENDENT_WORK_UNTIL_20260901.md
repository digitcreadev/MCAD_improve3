# Independent work allowed before 2026-09-01

This file lists work that does not require the blocked local-only bundle.

## Experimental/static lane

1. Audit current Git/static authorities on `benabib03`.
2. Freeze reproducible/pinned eMondrian build inputs. Do not build yet.
3. Prepare the external isolated-runtime overlay/compose plan without creating Docker objects.
4. Prepare R3-E executor, receipts, verifiers and deterministic contracts without backend I/O.
5. Run only static/unit/no-I/O tests: compile, schema/contract checks, receipt validators, deterministic mock tests.
6. Prepare but do not freeze the later materialization gate template.
7. Prepare an NH-R4 outline only; final preregistration waits for R3-F.

## Manuscript lane

1. Preserve the immutable A8 French core; do not overwrite it.
2. Integrate already frozen R3-D/D4 findings into a new manuscript descendant:
   - frozen protocol;
   - SAFE vs PERMISSIVE primary comparison;
   - 6/8 confirmed endpoints;
   - two SQL I/O endpoints not confirmed;
   - no global system-benefit claim.
3. Refine methods, terminology, reproducibility, limitations, threats to validity and cross-references using already frozen evidence.
4. Add explicit placeholders for R3-E, R3-F and NH-R4. Do not invent pending results.
5. Perform source/provenance audit only for claims already backed by frozen artifacts.
6. Do not use the obsolete root `MCAD_audited_real_main_fr.tex` as the new authority.

## Work intentionally deferred until bundle recovery

- final new-host E2 preflight;
- materialization authorization;
- `docker load`;
- SQL restore;
- isolated XMLA runtime creation;
- XMLA path validation;
- R3-E measurement;
- R3-E analysis/freeze;
- R3-F;
- NH-R4 execution;
- final manuscript integration of R3-E/NH-R4.
