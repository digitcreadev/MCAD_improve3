\begin{tikzpicture}[
  node distance=3.5mm,
  every node/.style={font=\scriptsize,align=center},
  layer/.style={draw,rounded corners=1mm,minimum width=0.94\linewidth,minimum height=9.5mm,inner sep=3pt},
  >=Stealth
]
\node[layer] (a) {\textbf{A. Scientific core}\\
Decision-analysis context modeled by CKG $\rightarrow$ objective into constraints + sufficient supports $\rightarrow$ null/partial/complete computability, relative to the query and session};
\node[layer,below=of a] (b) {\textbf{B. Contribution and formal mechanism}\\
$QP \rightarrow \mathrm{SAT} \rightarrow \mathrm{Real} \rightarrow C_{\mathrm{eval}} \rightarrow S^* \rightarrow \varphi,\Delta\varphi$; admissibility $\neq$ strategic contribution; redundancy relative to $O$ and state $H_t$};
\node[layer,below=of b] (c) {\textbf{C. Operationalization objects}\\
Virtual nodes as support specifications; CKG as active semantic memory; QP canonical; explanations through formal objects; decision traceability};
\node[layer,below=of c] (d) {\textbf{D. Action policy and system realization}\\
MCAD indicator $z_t$ $\rightarrow$ $\pi_{\mathrm{act}}$ $\rightarrow$ execute / notify / warn / guide / recommend / block; the ALLOW/BLOCK gate is a strict prototype instantiation};
\draw[->] (a)--(b); \draw[->] (b)--(c); \draw[->] (c)--(d);
\end{tikzpicture}
